package core;

public class Handler implements IHandler
{
    public void handle(ICommand command)
    {
        switch (command.command())
        {
            case "start":

            case "stop":

            case "choise":

            case "next":

            default:

        }
    }
}
